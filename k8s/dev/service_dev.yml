apiVersion: v1
kind: Service
metadata:
  name: unilever-service-dev
  namespace: unilever-dev  
spec:
  type: NodePort
  selector:
    app: unilever-app-dev
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007
